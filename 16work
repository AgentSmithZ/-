#include <math.h>
#include <iostream>
using namespace std;

int main() 
{
    float n = 3, i = 1, s = 0;
    do
    {
        s=s+(3*i+2);
        i=i+1;
    }
    while(i<=n);
    cout<<"S = ";
    cout<<s;
    system("pause");
    
    return 0;
}
