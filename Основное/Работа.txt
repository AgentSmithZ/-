#include <iostream>
#include <math.h>

using namespace std;

int main() {
    float x;
    cout<<"\nВведите символ";
    scanf("%d",&x);
    if (x<2)
        printf("Символ является числом в 2-ной системе счисления\n"
        "Символ является числом в 8-ной системе счисления\n"
        "Символ является числом в 10-ной системе счисления\n"
        "Символ является числом в 16-ной системе счисления");
    else if (x<8)
        printf("Символ не является числом в 2-ной системе счисления"
        "Символ является числом в 8-ной системе счисления"
        "Символ является числом в 10-ной системе счисления"
        "Символ является числом в 16-ной системе счисления");
        else if (x<10)
        printf("Символ не является числом в 2-ной системе счисления"
        "Символ не является числом в 8-ной системе счисления"
        "Символ не является числом в 10-ной системе счисления"
        "Символ является числом в 16-ной системе счисления");
        else if (x<16)
        printf("Символ не является числом в 2-ной системе счисления"
        "Символ не является числом в 8-ной системе счисления"
        "Символ не является числом в 10-ной системе счисления"
        "Символ не является числом в 16-ной системе счисления");
    

    return 0;
}


#include <iostream>
#include <math.h>

using namespace std;

int main() 
{
    float x;
    cout<<"\nСколько предметов сдал Иванов?"; cin>>x;
    if (x==1)
        cout<< "\nСлетает со стипендии!";
    else if (x==2)
        cout<< "\nОн не получит ничего!";
    else if (x==3)
        cout<< "\nОн получит стипендию! Ура! Ура!";

    return 0;
}


// Online C++ compiler to run C++ program online
#include <iostream>
#include <math.h>

using namespace std;

int main() 
{
    float x;
    cout<< "\nСколько лет вашему ребёнку?"; cin>>x;
    if (x<=2)
        cout<< "\nРебёнок идёт в ясли!";
    else if (x>=3 && x<=4)
        cout<< "\nРебёнок идёт в садик!";
    else if (x>=5 && x<=6)
        cout<< "\nРебёнок идёт в подготовительную школу!";

    return 0;
}


#include <iostream>
#include <math.h>

using namespace std;

int main() 
{
    float x,y,z;
    cout<< "\nВведите левую сторону треугольника: "; cin>>x;
    cout<< "\nВведите правую сторону треугольника: "; cin>>y;
    cout<< "\nВведите нижнюю сторону треугольника: "; cin>>z;
        if(x<z || x<y || z!=y)
            cout<< "\nТреугольник не строится!";
        else
            cout<< "\nТреугольник фигня, переделывай!";

    return 0;
}