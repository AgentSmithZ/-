#include <math.h>
#include <iostream>
using namespace std;

int main() {
    float a,b,c;
    cout<<"a = "; cin>>a;
    cout<<"b = "; cin>>b;
    cout<<"c = "; cin>>c;
    if(a>c)
    {
        cout<<"Кошка не подходит";
    }
    else
    {
        c<=b;
        if(c<=b)
        {
            cout<<"Кошка подходит";
        }
    }

    return 0;
}
